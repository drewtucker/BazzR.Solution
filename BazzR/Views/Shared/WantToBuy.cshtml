@{
  Layout = "_Layout";
}

<div class="col-sm-10">
  <h4 class="headerHighlight">Games up For Trade:</h4>
  @if (Model != null)
  {
    @foreach(var sell_transaction in Sell_Transaction.GetAll())
    {
      
    }
  }
</div>
